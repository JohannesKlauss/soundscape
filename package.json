{
  "name": "soundscape",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix && turbo run format",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "check": "turbo run check-types lint build test"
  },
  "devDependencies": {
    "turbo": "^2.7.6",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.6.1",
  "workspaces": [
    "apps/*"
  ]
}
